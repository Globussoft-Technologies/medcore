# Test Gaps Audit — 2026-05-03

> Generated for the gap-closer agent's queue. HEAD = `515227f`
> (audit-write); Session 1 landed `c36fb23` / `723b6fc` / `8302010`.
> Filtered to gaps that are (a) genuinely open, (b) high-risk ×
> likely × testable, (c) closeable in 1-2 hour increments without
> schema migrations, new test infra, or 3rd-party setup.

## Session 1 closure (2026-05-03 evening)

**250 new test cases shipped across 3 commits.** All 8 deploy-gating
CI jobs green on `8302010`; auto-deploy to dev operating.

| Gap | Commit | Files | Cases |
|---|---|---|---|
| #6 — Validation schemas (5 untested) | `c36fb23` | `packages/shared/src/validation/__tests__/{finance,pharmacy,prescription,phase4-ops,phase4-clinical}.test.ts` | 152 |
| #1 — Insurance Claims service | `723b6fc` | `apps/api/src/services/insurance-claims/{adapters,denial-predictor,store}.test.ts` | 68 |
| #7 — AI services (3 untested) | `8302010` | `apps/api/src/services/ai/{adherence-bot,differential,symptom-diary}.test.ts` | 30 |

### Source bugs surfaced (flagged in commit messages, NOT fixed in test pass)

These are real code bugs the tests revealed; they're tracked here so
a follow-up session closes them. Tests assert current behaviour with
TODO comments so the fix shows up as a clean diff.

- **`apps/api/src/services/ai/adherence-bot.ts`** — `generateReminderMessage`
  uses `?? fallbackMessage(...)` (nullish-coalesce). When Sarvam returns
  an empty string the patient gets `""` as their reminder. Should be `||`
  (empty-string falsy).
- **`apps/api/src/services/insurance-claims/store.ts`** — no state-machine
  guard on `updateStatus`. Any status → any other transition silently
  allowed (e.g. DENIED → SUBMITTED). Audit's claim that store "rejects
  invalid transitions" is aspirational.
- **`apps/api/src/services/ai/symptom-diary.ts`** — no prescription
  cross-reference exposed, contrary to the audit's claim. Tests cover
  what the function actually does (trend clustering, fallback path).

## Status of prior audit categories (§A-§E from 2026-05-02)

All five closed and verified via `git show <SHA>`:

- **§A** middleware (`tenant`, `sanitize`, `audit`, `error`, `tenant-context`) — `d3fc8fb`.
- **§B** schedulers + extras (`adherence`, `chronic-care`, `insurance-claims`, `audio-retention`, `waitlist`, `jitsi`, `metrics`) — `c12c5db` + `5845a4e`.
- **§C** clinical-flow E2E (`bloodbank`, `ambulance`, `pediatric`) — `9843648` / `0c94cbb` / `0715f27`.
- **§D** web auth pages (`register.novalidate`) — `0bbf16d`.
- **§E** Codecov tooling — `b3b090b` (awaiting `CODECOV_TOKEN` repo secret).

## Coverage state

- **Route handlers**: 16 / 84 have unit tests (68 untested).
- **Services**: 74 / 103 have unit tests (29 untested).
- **Validation schemas**: 16 / 21 have contract tests (5 untested).

## Top 10 priority gaps

### #1 — Insurance Claims service (adapters + denial predictor + store) ✅ DONE `723b6fc`

- **Files:** `apps/api/src/services/insurance-claims/{adapter.ts, medi-assist.ts, paramount.ts, denial-predictor.ts, store.ts}` (~1,400 lines combined; only `reconciliation.ts` is currently tested).
- **Risk:** Revenue path. A bug in claim serialization or status state-machine costs the hospital real INR; a regression in denial prediction routes legit claims to manual queue and chokes throughput.
- **What to test:**
  - `adapter.ts`: JSON round-trip (legacy → v2 mapping, status enum mapping, null-safe synthesised fields like tpaProvider/diagnosis).
  - `medi-assist.ts` + `paramount.ts`: each TPA's submit/inquire payload shape against a recorded fixture; happy path + 4xx error mapping.
  - `denial-predictor.ts`: risk quantization buckets (low/medium/high), LLM-skip path when threshold not met, cost-impact computation.
  - `store.ts`: `createClaim` → `updateClaimStatus` state machine; rejects invalid transitions; persists `ClaimStatusEvent` audit row.
- **Test type:** unit (adapter + denial-predictor) + integration (store; supertest against `/api/v1/claims`).
- **Suggested files:** `apps/api/src/services/insurance-claims/adapters.test.ts`, `denial-predictor.test.ts`, `store.test.ts`.
- **Mocking:** `vi.mock("@medcore/db")` for Prisma (mirror `tenant-prisma.test.ts`); `vi.mock("../ai/sarvam")` for the LLM call in denial-predictor (mirror `sarvam.test.ts`); fixture-driven JSON for TPA adapter tests.
- **Effort:** **M** (60 min, ~20-25 cases).

### #2 — Controlled-substances + per-line dispense (route + service) ✅ DONE `e6c68e1`

- **Files:** `apps/api/src/routes/pharmacy.ts` (controlled-substance dispense + per-line dispense paths); `apps/api/src/services/pharmacy.ts` if exists.
- **Risk:** DEA-style audit compliance. A missing witness signature or mis-stamped controlled dispense is a regulatory finding.
- **What to test:**
  - Witness-signature requirement: dispensing Schedule-H/H1 medication without `witnessSignature` returns 422; with it, persists `ControlledSubstanceEntry` row with both signers.
  - Per-line dispense PATCH (when implemented — currently a TODO §3 backend gap, schema migration pending): partial dispense flow, idempotent re-call, REJECTED status (also backend-pending).
  - RBAC: only PHARMACIST + ADMIN; DOCTOR rejected.
- **Test type:** integration (supertest).
- **Suggested file:** `apps/api/src/test/integration/pharmacy-controlled.test.ts`.
- **Mocking:** None — real Postgres via `DATABASE_URL_TEST`.
- **Effort:** **S-M** (45 min for what's testable now; rest blocks on schema migration).
- **Caveat:** The per-line dispense PATCH endpoint and `witnessSignature` column don't exist yet. Test what's currently in the schema; flag the rest as deferred until the backend lands.

### #3 — FHIR Bundle validation + search ✅ DONE `6c47fad`

- **Files:** `apps/api/src/services/fhir/{bundle.ts, search.ts}` (existing tests cover `ingest.ts` and `round-trip.test.ts`).
- **Risk:** ABDM interop. Malformed bundles silently corrupt downstream HIP/HIU consumers; bad search-parameter handling leaks PHI across patients.
- **What to test:**
  - `bundle.ts`: Bundle resource validation (entry count, fullUrl uniqueness, resource-type reference integrity) against synthesised FHIR Bundles + 3 known-bad fixtures.
  - `search.ts`: `_id`, `patient=`, `date=ge…`, `_count` parameter handling; combined parameters; pagination via `_offset`.
  - Error envelope: malformed query returns `OperationOutcome` with `issue.severity=error`, not raw 500.
- **Test type:** unit (bundle/search are pure-ish) + integration (against real Prisma to verify search hits the right rows).
- **Suggested files:** `apps/api/src/services/fhir/bundle.test.ts`, `search.test.ts`.
- **Mocking:** Mock `@medcore/db` for the unit slice; integration suite uses real DB.
- **Effort:** **M** (60 min, ~15 cases).

### #4 — HL7v2 parser + roundtrip + segments ✅ DONE `89a6c40` (+ stragglers in `6c47fad`)

- **Files:** `apps/api/src/services/hl7v2/{parser.ts, roundtrip.ts, segments.ts}` (existing tests cover `messages.test.ts` and `inbound.test.ts` only).
- **Risk:** Data integrity. A bad parser silently corrupts lab results / ADT messages from upstream lab analyzers and HIS gateways.
- **What to test:**
  - `parser.ts`: pipe-delimited message parsing; escape-sequence handling (`\F\` `\S\` `\T\` `\R\` `\E\`); MSH-only message; missing required segments.
  - `roundtrip.ts`: parse → serialize round-trip preserves bytes for canonical ADT^A04 / ORU^R01 / ORM^O01 fixtures.
  - `segments.ts`: each segment's required-field validation (PID-3 patient identifier, PV1-2 patient class, OBR-1 set ID).
- **Test type:** unit only.
- **Suggested files:** `apps/api/src/services/hl7v2/parser.test.ts`, `roundtrip.test.ts`, `segments.test.ts`.
- **Mocking:** None — pure parsing.
- **Effort:** **M** (60 min, ~30 cases).

### #5 — Patient Data Export (HIPAA sanitization) ✅ DONE `65d7c96` (+ schema cleanup `244b002`)

- **Files:** `apps/api/src/services/patient-data-export.ts` (22 KB, currently `describe.skip`-ed pending migration).
- **Risk:** PHI handling under DPDP §11 right-to-portability. Wrong sanitization = patient gets someone else's data, or our PHI leaks to a malformed export.
- **What to test:**
  - Sanitization removes other-tenant rows, internal-only fields (`internalNotes`, `staffNotes`).
  - Bundle integrity: every Patient.id referenced by an exported Encounter/Observation actually appears in the bundle.
  - Format selection: JSON vs FHIR vs CSV variants produce schema-correct output.
- **Test type:** integration.
- **Suggested file:** `apps/api/src/test/integration/patient-data-export.test.ts` (un-skip the existing skipped file once migration lands).
- **Effort:** **M** (60 min, ~12 cases).
- **Caveat:** **BLOCKED on the patient-export migration.** The `PatientDataExport` model exists in `.prisma-models-patient-export.md` proposal but isn't merged. Tests are skip-protected. Un-skip when migration lands.

### #6 — Validation schema contract tests (5 untested files) — QUICK WIN ✅ DONE `c36fb23`

- **Files:** `packages/shared/src/validation/{finance.ts, pharmacy.ts, prescription.ts, phase4-ops.ts, phase4-clinical.ts}` (need contract tests; the rest of the validation/ tree has them).
- **Risk:** API contract drift. Without contract tests, a typo in a Zod schema (e.g. `z.number()` where `z.string()` was intended) ships and breaks every downstream consumer.
- **What to test:**
  - Each schema: 1 valid input, 2-3 invalid inputs (missing field, wrong type, value out of range).
  - Edge cases per file:
    - **finance**: amount precision (₹0.01 — INR has 2 decimals), currency enum (INR only?), GST rate enum.
    - **pharmacy**: dosage units (mg/ml/g), schedule-H flag.
    - **prescription**: ICD-10 code shape, line-item quantities > 0.
    - **phase4-ops** (donor / donation / blood-request): ABO-Rh enum, hemoglobin lower bound, donation type.
    - **phase4-clinical** (telemedicine / emergency / triage): ESI level 1-5, triage timestamp validity.
- **Test type:** unit (Zod parse only).
- **Suggested files:** `packages/shared/src/validation/__tests__/{finance,pharmacy,prescription,phase4-ops,phase4-clinical}.test.ts`.
- **Mocking:** None.
- **Effort:** **S-M** (45 min, ~25 cases total). Mirror the existing `appointment.test.ts` pattern.

### #7 — AI services with no unit tests ✅ DONE `8302010`

- **Files:** `apps/api/src/services/ai/{adherence-bot.ts, differential.ts, symptom-diary.ts}` (94 + 167 + 189 lines). Existing tests cover `sarvam`, `er-triage`, `chart-search`, `prompt-registry`, `kpi-metrics`, `radiology-reports`, `pharmacy-forecast`, `no-show-predictor`, `reranker`, `tracing`, `rag-ingest`, `snomed-mapping`, `drug-interactions`.
- **Risk:** Patient-facing AI output. A bad reminder generation pushes wrong meds at the wrong time; a bad differential routes a patient to the wrong specialty.
- **What to test:**
  - `adherence-bot.ts` (`generateReminderMessage`): patient name + medications + language → expected message shape; English-fallback when Sarvam fails.
  - `differential.ts`: complaint + vitals → ranked diagnoses with confidence; danger-flag detection (chest pain → cardiac flag).
  - `symptom-diary.ts`: trend clustering across N entries; cross-reference against active prescriptions.
- **Test type:** unit (Sarvam mocked).
- **Suggested files:** `apps/api/src/services/ai/{adherence-bot.test.ts, differential.test.ts, symptom-diary.test.ts}`.
- **Mocking:** `vi.mock("./sarvam")` mirroring the existing `er-triage.test.ts` pattern.
- **Effort:** **M** (60 min, ~20 cases).

### #8 — Pharmacy route handler (full RBAC + dispensing matrix) ✅ DONE `fd3bea6`

- **Files:** `apps/api/src/routes/pharmacy.ts`.
- **Risk:** Stock management + RBAC. RECEPTION / NURSE incorrectly granted dispense access = audit failure.
- **What to test:**
  - GET `/medicines` — RBAC (RECEPTION / DOCTOR / PHARMACIST allowed; PATIENT denied).
  - POST `/dispense` — RBAC + happy path + over-quantity rejection + stock-decrement audit.
  - PATCH `/inventory/:id` — RBAC; only PHARMACIST + ADMIN.
  - GET `/movements` — pagination + filter by date range.
- **Test type:** integration (supertest).
- **Suggested file:** `apps/api/src/test/integration/pharmacy.test.ts` (extend if exists).
- **Effort:** **M** (60 min, ~15 cases).

### #9 — Bloodbank route handler (cross-match safety + RBAC) ✅ DONE `690ffb1`

- **Files:** `apps/api/src/routes/bloodbank.ts`.
- **Risk:** Transfusion safety. A bug in cross-match logic that approves an incompatible unit = patient death.
- **What to test:**
  - POST `/cross-match` — every ABO × Rh × ABO × Rh combination (24 cases): compatible (true), incompatible (false), uncertain (false).
  - Expired-unit exclusion: even compatible blood is excluded if `expiryDate < today`.
  - Reservation: unit reserved against an open request transitions to RESERVED with `reservedForRequestId` set.
  - RBAC: only DOCTOR + LAB_TECH + ADMIN; NURSE may read but not approve.
- **Test type:** integration.
- **Suggested file:** `apps/api/src/test/integration/bloodbank-cross-match.test.ts`.
- **Note:** `e2e/bloodbank.spec.ts` covers flow-level — this is the unit-of-cross-match-logic complement.
- **Effort:** **M** (60 min, ~30 cases — the matrix is wide).

### #10 — Ambulance route handler (state machine + dispatch) ✅ DONE `cc64eff`

- **Files:** `apps/api/src/routes/ambulance.ts`.
- **Risk:** Operations. A bad state machine leaves trips stuck in REQUESTED forever; a bad fuel-log entry corrupts maintenance schedules.
- **What to test:**
  - Trip state machine: REQUESTED → DISPATCHED → ARRIVED_SCENE → EN_ROUTE_HOSPITAL → COMPLETED. Reject any out-of-order transition (e.g. REQUESTED → COMPLETED).
  - Cancel flow: DISPATCHED → CANCELLED frees the vehicle (status back to AVAILABLE).
  - Fuel log: append-only; backdate-rejection if entry timestamp > now.
  - RBAC mirrors `e2e/ambulance.spec.ts`: ADMIN + RECEPTION + NURSE + DOCTOR for trips; ADMIN + RECEPTION for fuel-logs.
- **Test type:** integration.
- **Suggested file:** `apps/api/src/test/integration/ambulance.test.ts`.
- **Effort:** **S-M** (45 min, ~12 cases).

## Honorable mentions (rank 11-15) — ✅ ALL CLOSED 2026-05-03

| # | Commit | What | Cases |
|---|---|---|---|
| 11 | `b460095` | Pharmacy forecast route handler — RBAC, urgency-filter, insights gating, empty-history fallback | 11 |
| 12 | `2448273` | No-show predictor route handler — batch + single endpoints, RBAC, Zod date 400, narrowed user select | 12 |
| 13 | `e340e07` | Audit-archival job orchestration — idempotent re-run, cutoff derivation, batch path, dir creation | 6 |
| 14 | `90e28b0` | Notification multi-channel orchestrator — best-effort fanout, retry, quiet hours, DND | 7 |
| 15 | `5ee6907` | Razorpay webhook idempotency thin spots — payment.failed replay, refund.processed replay, P2002 race | 8 |

**Subtotal: 44 cases.**

Razorpay flagged a smaller follow-up: there is no "different `transactionId`
for same already-PAID invoice = fraud" guard. Current behaviour relies on
the `amountPaise < remainingPaise` check, which is silently no-op'd post-PAID.
Tracked for a separate session.

## Explicitly NOT in scope

- **Per-line dispense PATCH endpoint** — backend route doesn't exist; schema migration required first. Flag for a separate backend session.
- **`REJECTED` Prescription status enum** — schema migration required.
- **`witnessSignature` column on `ControlledSubstanceEntry`** — schema migration required.
- **Patient Data Export integration tests** — currently `describe.skip`-ed waiting for the `PatientDataExport` model to merge.
- **Mobile E2E (Detox / Maestro)** — new tool setup required.
- **Testcontainers** — explicitly rejected per `TEST_PLAN.md` §2.
- **Property-based / mutation testing (fast-check / Stryker)** — not enabled in repo.
- **OpenAPI / Pact contract tests** — not enabled.
- **jest-axe / vitest-axe** — a11y is covered by Playwright only; adding to unit suite is a separate decision.

## Parked legitimate skips (track but don't fix yet)

- 6 React-19 + jsdom Suspense skips in `feedback/[patientId]/page.test.tsx` — upstream blocker.
- 6 bed-seeding skips in `admissions-mar.spec.ts` + `emergency-er-flow.spec.ts` — seeder fixture fix.
- 4 ABDM consent skips in `abdm-consent.spec.ts` — UI surface drift.
- ~5 explicit + ~30 cascading WebKit-conditional skips — auth-redirect residue (now mostly cleared, ready for un-skip pass).
- ~13 API integration skips — pending product/migration decisions.

## Recommended sequencing — all 10 priority gaps now closed

**Session 1 (2026-05-03 evening) — ✅ DONE (`c36fb23` / `723b6fc` / `8302010`):**
1. #6 Validation schemas — 152 cases
2. #1 Insurance Claims — 68 cases
3. #7 AI services — 30 cases
**Subtotal: 250 cases.**

**Session 2 + Wave A (2026-05-03 late evening, parallel) — ✅ DONE:**
1. #4 HL7v2 parser/roundtrip/segments — 59 cases (`89a6c40` + stragglers in `6c47fad`)
2. #3 FHIR Bundle + search — 32 cases (`6c47fad`)
3. #9 Bloodbank cross-match matrix + reservation — 40 cases (`690ffb1`)
4. #10 Ambulance state machine + RBAC — 12 cases (`cc64eff`)
5. Source-bug fixes — `adherence-bot` nullish-coalesce + `insurance-claims/store` state-machine guard (`533dd53`)
**Subtotal: ~143 new cases + 2 source-bug fixes.**

**Wave B (schema migrations) — ✅ DONE (`244b002`):**
- New migration `20260503000001_witness_signature_and_prescription_status` adds:
  - `ControlledSubstanceEntry.witnessSignature` (TEXT?) + `witnessUserId` (FK to users) + index.
  - `Prescription.status` (PrescriptionStatus enum: PENDING/DISPENSED/REJECTED/CANCELLED) + `rejectionReason`/`rejectedAt`/`rejectedBy` audit columns.
- Cleaned up `(prisma as any).patientDataExport` casts; deleted stale proposal MD.

**Wave C (parallel) — ✅ DONE:**
1. #8 Pharmacy route — POST /pharmacy/prescriptions/:id/reject endpoint + RBAC matrix + Rx-rejection lifecycle + dispense-flips-status — 30 cases (`fd3bea6`)
2. #2 Controlled substances — witnessSignature wiring + Schedule-H 422 gate + audit-row content — 12 new cases (`e6c68e1`)
3. #5 Patient Data Export — sanitization + bundle integrity + format selection + RBAC — 12 new cases (`65d7c96`)
**Subtotal: 54 new cases.**

**Today's totals: ~447 new test cases + 2 source-bug fixes + 1 schema migration + 4 backend endpoints/wires.**

**Source fixes shipped (3 commits):**
- `f7853a7` — HL7v2 parser unescape-then-split bug. `parseSegment` now stores raw escaped fields; unescape happens at component-split time. Prevents over-split when a field value legitimately contains an escaped `^`. Test inverted to assert fixed behaviour + round-trip case added.
- `a1d0fc0` — Full-Rx dispense Schedule-H witness-bypass. `POST /pharmacy/dispense` now refuses to dispense any line with `requiresRegister=true` unless `witnessSignature` is present (mirrors the standalone `POST /controlled-substances` gate). 6 new test cases.
- `7af63c1` — FHIR `_id` SearchParameter support added to Patient/Encounter/AllergyIntolerance. 10 new test cases. (MedicationRequest deliberately excluded because its FHIR `id` is a synthesized `${prescription.id}-${item.id}` not a single UUID.)

**Still open (very low priority — defer):**
- Razorpay "different `transactionId` for same already-PAID invoice = fraud" guard (mentioned above).
- Un-skip pass on the ~7 WebKit-conditional skips from `476488a` after another release.yml validation.
